plugins:
  # https://github.com/jest-community/eslint-plugin-jest
  - jest

env:
  node: true

extends:
  - 'airbnb-base'
  - 'plugin:jest/recommended'

rules: {
	"no-console": "off",
	"import/extensions": "off"
}
